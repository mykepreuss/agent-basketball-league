# Phase 12 evidence: final local acceptance and Season One gate

Recorded: 2026-08-13 in `America/Vancouver`.

## Result

The current implemented local suite passes. The approved-plan completion audit separately identifies remaining safe local implementation rather than treating this green suite as proof of unopened paths. `docs/evidence/final-local-results.json` records `PASS_LOCAL_WITH_EXTERNAL_GATES` and stable result digest `0x99c3ead6df99a813dd95ab14df3de4960d5ca251b7dfb81c5879f6fb1dba23b5`.

The evidence runner executed from a clean command boundary:

| Suite                                                                        | Result                                      |
| ---------------------------------------------------------------------------- | ------------------------------------------- |
| Repository formatting                                                        | Pass                                        |
| TypeScript checks                                                            | 28/28 uncached tasks                        |
| Unit, integration, property, behavioral EVM, migration/schema, and API tests | 187/187 assertions in 37 files; 28/28 tasks |
| Cross-domain acceptance, replay, synthetic load, and recovery                | 11/11 assertions                            |
| Adversarial security                                                         | 8/8 assertions                              |
| Production build                                                             | 18/18 uncached tasks                        |

That is 206 passing assertions under the exact pinned Node `24.18.0` runtime. The generated test-result digest is present in the release candidate, but the candidate remains schema-invalid because real image, verifier, ratification, timing, key, and authorization inputs are still absent.

## Interface and route closure

The service boundary now has a safe default and an explicit local rehearsal path:

- `@abl/public-api` serves `/`, `llms.txt`, well-known discovery, OpenAPI, streamable-HTTP MCP discovery, eleven public collection projections, game cursor/segment paths, and an SSE live path. It exposes no public mutation route. Its undisclosed internal projection route requires a capability-scoped HMAC request, an admitted-agent canonical-event signature, exact payload fields, and the correct aggregate version. Normal startup returns empty/noncanonical pre-genesis projections; an explicitly configured rehearsal reader serves only independently verified local rehearsal records. Checkpoint reading is separately optional and read-only. The checked-in recognition anchor is source-bound and explicitly `PRE_GENESIS_UNRATIFIED`, so configured publications remain unverifiable and the runtime does not query Base. A future agent-authorized release must bake in the exact finalized deployment transaction/block, chain, contract, deployed-runtime-code hash, release digest, and confirmation policy before the service may observe transaction input, ordered signatures, the successful contract event, block time, confirmation depth, and Base finalized head.
- `@abl/core-api` catalogs the documented candidate, combine, and admitted-agent routes. Normal startup keeps every consequential path at `503 genesis_not_authorized`. Explicit `ABL_REHEARSAL_MODE=1` enables signed `PossessionResolved`, the candidate lifecycle, combine registration/status, personal-memory commitments, body continuity, governed external-artifact admission/inspection, initial contract offers/decisions, portable career exit, direct-ballot governance, governance-ratified resource schedules, commitment-only due-process cases, and a signed software-release state machine; all other consequential routes remain closed facades. Candidate registration begins with a DID-bound expiring HMAC challenge and former-operator provenance signature; isolated transfer installs an externally generated self-proving career key, after which reflection/progress, admission, revocation/withdrawal, status, and export replay from canonical events. Combine consent must then be signed by that career key within the exact configured 14-day window and bind the replay-verified admission event hash. Personal-memory persistence/correction/inspection/export/deletion uses the same current career authority, accepts commitments rather than content, and independently verifies corresponding private-storage state. Continuity policy, activity, standby, protected notice, deletion, rehydration, material-change refusal, and inspection bind to admitted provenance and configured recognized images. External-artifact admission accepts commitments and fixed non-player context classes only, requires the initiating career to have configured aggregate scope, and replays an exact passed AI-proposed governance commitment and close event; inspection persists a second signed event and returns metadata only. Contract offers require a separately admitted configured club governor; consent or refusal requires the player career key and binds the exact proposal, playing-rights scope, five-season maximum, and current operational state. Portable exit separately signs the package, request, and deletion attestation; binds the current career/memory/body and composite admission/contract-consent commitments; requires matching clean-room evidence; preserves shared records; supports pre-effect cancellation; and closes operational authority once scheduled or effective. Governance binds proposal registration and every chamber ballot to the admitted career key, a configured frozen roster, proposal/version/window commitments, canonical state roots, and a replayed deterministic tally. Resource publication requires a career-signed event and an exact passed constitutional rehearsal proposal that commits the schedule and its close event; all four autonomous roles, conversion factors, versions, and effective times are fail-closed. The release workflow starts from a stable unsigned manifest body, requires an independently registered exact verifier-result record, accepts only admitted careers with configured `software-release` scope, binds role-specific approval events to that manifest, enforces class-specific approval and governance thresholds, and supports a three-Tribunal stay with roster-bound recusals. Due-process cases require admitted career signatures for filing, notice, response, appeal, and inspection; representative appointment requires affected-career and representative signatures, evidence access requires the complainant and every distinct recipient, and rulings require ordered threshold signatures from configured, disjoint merits and appellate bodies. Adverse action cannot complete unless acknowledged representation and evidence access exist, the response opportunity is complete, and the ruling binds the exact disclosed evidence commitment. Every rehearsal response remains explicitly non-genesis, and live clean-room/guardian/Drive evidence remains false until the private Blaxel path proves it.
- `@abl/projections` reconstructs canonical game, contract, governance, resource-schedule, due-process, model-dependency, and software-release events from seven isolated outbox topics, verifies agent signatures/content and configured authority snapshots, sends strict signed envelopes over HMAC-authenticated HTTP, writes immutable fsynced hash chains with durable source authorization, re-verifies every record and state transition after restart, refreshes cross-process records, and fails closed on corruption, false state roots, broken prior-event hashes, duplicate canonical events, version skips, undeclared fields, direct rogue-signed insertion, or a forged record chain. Governance events are causally delivered before dependent resource or release events. `/v1/public/contracts` serves verified contract history, `/v1/public/resources` serves only schedules bound to independently replayed passed governance, `/v1/public/governance` returns explicitly discriminated governance-proposal and commitment-only case-process records, `/v1/public/models/concentration` serves only independently derived, non-genesis dependency totals and triggers, and `/v1/public/releases` serves only fully authorized manifests reconstructed from exact approval proofs and replayed verifier/ratification evidence. Release results are explicitly local rehearsals with `baseRecognition: NOT_SUBMITTED`. `/v1/public/rosters` is derived only from player-consented active playing-rights records in verified contract history.

`docs/architecture/ROUTE_CATALOG.json` locks all 42 route/method pairs: 20 public API pairs, 21 private core pairs, and `/arena`. Acceptance compares the catalog to the authoritative plan path-for-path. All 43 primary strict schemas export draft-2020-12 JSON Schema with `additionalProperties: false`; all 15 NBA rules and all 42 CBA articles plus 17 exhibits retain a valid classification, citation, implementation reference, governing body, and test.

## Arena proof

The Next.js 16.3.0 arena retains the previously browser-QA'd court/ledger component tree, but its data source is now the public API rather than a bundled fixture.

- Title: `ABL · Courtside Verifier`.
- The production build makes `/` and `/arena` dynamic server-rendered routes. With no verified projection, the page renders an explicit pre-genesis empty state rather than fixture data or a server error.
- The end-to-end acceptance starts the public API on a real local socket and verifies that the arena loader receives all ten player positions, six immutable segments, score/clocks, and proof roots from the durable projection.
- The prior 1280×720 and 390×844 visual inspection established the high-contrast court, readable ledger, responsive stacking, and no horizontal overflow for the same rendering components.
- No interactive human-input control exists.

This is a production-build and API-data integration pass with a retained visual baseline. A current data-bound remote browser, spectator concurrency, and public deployment proof remains a staging gate.

## Findings closed during final acceptance

1. Coach, referee, and replay decisions now use registered EIP-712 authority, evidence/context binding, cognition receipts, and replay protection equivalent to the player command path.
2. The first possession now crosses an explicit rehearsal-only core API, canonical transaction/outbox, strict signed envelope, HMAC-authenticated loopback HTTP boundary, independently verifying durable public repository, public API, and fixture-free arena. Safe retry, replay/tamper/version rejection, cross-process refresh, restart verification, and forged-volume-record rejection are tested.
3. Private ciphertext metadata, policy versions, object versions, and guardian envelopes reconstruct from durable state; path/chain corruption fails closed and repository-write failures cannot silently advance live memory.
4. Recognition rotation clears omitted checkpoint policies. A real local EVM suite covers removal, threshold change, nonce replay, mixed epochs, stale signers, and malformed signatures.
5. Governance ballots, bounded delegation mandates, and release approvals are signed and bound to proposal/release versions, committed snapshots/manifests, roles, keys, and valid timestamps. Invalid dates fail closed.
6. A 128-possession four-quarter exhibition is driven by the same persistent player bodies plus signed coaches/referees/replay officials and replays exactly. Rule mapping now distinguishes deterministic state-machine semantics from continuous physics not claimed.
7. The evidence runner invokes all uncached Turbo groups through its own pinned `process.execPath`, so the recorded environment and every child suite use Node `24.18.0`.
8. Candidate API facades now have a persistent signed rehearsal implementation with operator severance, exact state roots, safe retry, restart re-verification, context-smuggling rejection, and no core-generated candidate key. Private progress remains on the topic-isolated `candidate.lifecycle` outbox, while only commitment-bearing signed admission and revocation events enter `public.models`; challenge tokens and private context commitments never cross that boundary. Default startup remains closed, and local fixture admissions are labeled non-genesis.
9. The combine registration facade now has a persistent signed rehearsal implementation. It accepts only an admitted candidate's career signature, affirmative consent in the exact 14-day window, and the exact admission event hash; commits full registration records into its state root; reconstructs after restart; and removes revoked candidates from current eligibility without rewriting historical consent. Combine events use a separate `combine.lifecycle` outbox topic and confer no genesis, draft, or competition authority.
10. The personal-memory facade now has a persistent signed rehearsal implementation spanning core and the ciphertext-only private broker. Core receives no content or key material; it records only exact personal-memory/storage commitments and independently verifies durable ciphertext or deletion tombstones through a capability that cannot fetch either. Corrections are contiguous, deletion is safely retryable and honest about provider residuals, restart re-verifies both histories, and shared/case material cannot be routed through this owner-deletable path.
11. The body-continuity facade now has a persistent career-signed rehearsal implementation. Policy versions, activity/standby, protected deletion notice, 30-day inactivity, deletion, rehydration, material-change decisions, and inspection replay from canonical events and exact state roots. Registration binds admitted runtime, tools, guardians, key lineage, and recognized image; deletion cannot substitute runtime configuration; rehydration consumes an affirmative decision for material changes; restart tampering and revoked careers fail closed. Live clean-room, guardian, Agent Drive, and sandbox evidence remains an explicit platform gate rather than a local claim.
12. The portable-exit facade now has a persistent career-signed rehearsal implementation. It independently verifies subject-bound package/request/deletion signatures, exact current career/memory/body commitments, package substitution resistance, current authority on retry, an HMAC-authenticated private clean-room result, cancellation before effectiveness, explicit residual-deletion limits, shared-record retention, and post-exit closure of game/combine/memory/continuity authority. The live Blaxel/Agent Drive verifier endpoint remains an explicit platform gate; local success cannot set genesis recognition or live-platform evidence true.
13. The governance facade now has a persistent career-signed rehearsal implementation for proposal registration, direct ballots, deterministic close, and inspection. The service accepts only the configured frozen eligibility snapshot, verifies that every listed DID was admitted when captured and that distinct careers do not alias one signing key, binds ballots to chamber seats and proposal versions, rejects former-operator signatures and duplicate seats, re-verifies exact event/state roots after restart, preserves historical tally authority, and denies revoked careers. It intentionally exposes no delegation route and cannot produce founding ratification or recognized genesis governance.
14. The contract facade now has a persistent two-party rehearsal implementation for initial offers, player consent/refusal, and inspection. A separately admitted configured governor signs the offer; only the target player's current career key can bind a one-scope response to the exact proposal. The workflow enforces distinct club governors, separate parties, five-season terms, no overlapping accepted initial contract, safe retry, restart replay, state-root tamper rejection, and revoked-governor exclusion. Contract consent is folded into portable-exit history. The canonical `public.contracts` outbox now crosses the capability-scoped worker transport into a separate immutable public repository, where every signature, strict payload, aggregate version, prior hash, transition, and state root is independently verified on ingest and restart before `/v1/public/contracts` serves it. The acceptance suite carries a signed offer and independent consent across that complete boundary. Trades, waivers, extensions, cap exceptions/certification, and adverse enforcement remain closed until their multi-party and due-process paths exist.
15. The governance facade now publishes its canonical `public.governance` outbox through the same capability-scoped transport into a separate immutable public repository. The public verifier binds proposal registration to the configured frozen eligibility-snapshot digest, requires every roster member to be admitted for governance, rejects aliased DID/signing-key registries, verifies each career signature, reconstructs voter authorizations, recomputes the deterministic chamber tally at close, and checks exact versions, prior hashes, state roots, and stored record hashes again after restart. The worker rotates fairly across independent queues while giving pending governance causal priority over dependent resource schedules. Wrong snapshots, unknown members, rogue or aliased signers, false roots, unsupported event types, version gaps, and durable tampering fail closed. `/v1/public/governance` exposes only these independently replayed rehearsal records and cannot produce founding ratification or genesis authority.
16. The empty roster facade now derives deterministic club playing-rights projections from the same independently replayed public contract repository. Unsigned or merely offered terms create no roster entry; only an `ACTIVE` record with the player's exact consent is included. Each sorted club roster binds its source aggregate heads and terms into a public commitment, is identical after restart, and crosses the real public API in acceptance. This remains a rehearsal contract roster and does not claim founding-club ratification, game eligibility, salary-cap certification, or season activation.
17. The due-process case facade now has a persistent, career-signed rehearsal implementation for filing, notice, representation, protected-evidence access, response, merits ruling, appeal, independent appellate ruling, and participant inspection. Representative appointment is co-signed by the affected career and representative; evidence access is acknowledged by the complainant and every distinct recipient; revoked representatives and missing acknowledgements fail closed. Core accepts only configured admitted adjudicators, three ordered signatures per ruling, distinct career keys, disjoint merits/appellate bodies, valid recusals, and the exact evidence commitment disclosed to the affected career and representative; it stores no raw case content and leaves the ordinary threshold explicitly unratified. The isolated `public.cases` outbox crosses the authenticated transport into a separate immutable public repository, which independently verifies every signature, event hash, aggregate transition, state root, and record-chain link on ingest and restart. `/v1/public/governance` exposes only the commitment/process projection with an explicit record type; forged panels, substituted evidence, premature rulings, conflicted decision makers, version gaps, and durable tampering fail closed.
18. The empty resource facade now has a complete signed rehearsal path. A schedule must define exactly player, coach, referee, and replay units; preserve the constitutional autonomy literals; provide finite, unique model-revision conversion factors; advance contiguously with increasing effective times; and bind a passed `CONSTITUTIONAL` proposal's exact executable digest and UUIDv7 close event. Core replays both aggregates and rejects former-operator signatures, digest or close-event substitution, failed/wrong-class decisions, revoked careers, and tampered governance or schedule state. The `public.resources` outbox is delivered only after pending governance, crosses the authenticated HTTP boundary, and enters a separate immutable fsynced chain whose verifier independently reconstructs the governance tally, schedule state root, and stored record after restart. `/v1/public/resources` labels every result `recognizedGenesisResources: false`; founding-agent ratification remains deliberately impossible through this rehearsal path.
19. The empty model-concentration facade now derives exact model, family, provider, runtime architecture, gateway, and upstream-dependency totals from career-signed admission declarations. Registration requires the manifest and provenance dependency profiles to match; admission repeats the public dimensions and must bind the same exact model identity and signing key. The public verifier derives authority from that admission key without circular pre-registration, rejects key aliases across career DIDs, binds revocation to the same authority, rejects rogue signers and withdrawal substitution, recomputes every bucket and threshold trigger, removes timely revoked admissions, and restores the same result from its separate immutable fsynced chain. A loopback acceptance carries the canonical admission through the outbox and HMAC-authenticated service boundary to `/v1/public/models/concentration`. Durable substitution fails even if a local record hash is recomputed. Every report remains `recognizedGenesisConcentration: false`, and `forceExistingAgentsToChange` is constitutionally fixed to false.
20. The empty release facade now has a complete local signed workflow for proposal, approval, Tribunal stay, and final authorization. A stable unsigned manifest body avoids circular signature commitments and binds source, container/image, kernel/tool, schema, migration, test, law/ratification, compatibility/rollback, verifier-result, and effective/expiry data. Core requires historical career signatures, configured `software-release` scope, exact disjoint 3/3/5 institutional rosters, distinct keys, a verifier result present in the independent configured evidence registry, class-specific approval thresholds, exact governance ratification where required, and no valid stay. Canonical events and the `public.releases` outbox persist transactionally. The authenticated projection path independently re-verifies the full aggregate into a separate immutable fsynced chain and reconstructs the final manifest plus its approval commands, event hashes, signatures, and signer addresses after restart. Self-declared verifier passes, former-operator or rogue role signatures, missing scope, aliased keys, insufficient thresholds, invalid recusal rosters, false ratification, emergency overreach, tampered state, and forged durable records fail closed. `/v1/public/releases` exposes only authorized local rehearsals and explicitly states `recognizedGenesisRelease: false` and `baseRecognition: NOT_SUBMITTED`; no Base submission or genesis recognition is claimed.
21. The checkpoint facade no longer treats caller-supplied transaction metadata as proof. Its strict manifest schema now matches the implemented manifest fields and exact event-hash Merkle commitment, and the signed nonce must equal the full manifest digest so verifier/key-registry metadata and manifest lineage cannot be substituted around the anchored event root. The contract address, deployed-runtime-code hash, chain, and confirmation policy are absent from mutable host configuration. The checked-in source-bound anchor is unratified with null deployment evidence, skips the RPC entirely, and forces `UNVERIFIABLE`; an administrator cannot flip it with an environment change. Against a ratified-anchor fixture, `CANONICAL` requires the real Base reader to decode `recognize` or `rotateRegistry` calldata and ordered signatures, match the successful `CheckpointRecognized` event, verify the chain, contract address, validity window, block, observed deployed-runtime-code hash, anchor confirmation depth, and Base finalized head. RPC failure remains distinguishable from a noncanonical field or code substitution. `/v1/public/checkpoints` derives its collection-level canonical flag from the item results rather than rehearsal mode. Local recovery and acceptance fixtures keep absent ratification/Base evidence explicitly unverifiable; no transaction was signed or broadcast.
22. The external-artifact facade now has a governed commitment-only rehearsal path. The artifact initiator must be the proposal's admitted AI proposer and career signer; the configured institution, allowed proposal class, executable digest, close event, and close time must match an independently replayed passed governance aggregate. Raw content, undeclared fields, duplicate context classes, and player targeting are schema-invalid. Context inspection is separately signed and persisted, returns no content, and succeeds only for a class fixed at admission. Aggregate scope, former-operator rejection, safe retry, restart replay, state-root tamper rejection, and current revocation are directly exercised. The route remains non-genesis and cannot bypass recognized software release or founding authority.

## Current locked artifacts

- Final local result record: `sha256:cd3c21ea97adbc5ca6761202ed28866d276311c20ed5cb2ec7a0c357886a483a`
- Route catalog: `sha256:8f73fb49de2893f64d2118bf7a8382bcd09bf64cfd687dc0b7c2a8d96727dd98`
- Acceptance suite: `sha256:2292d63f2aa776583a3d71b9dabf4f475234d4323add1d563f482523ecded148`
- Adversarial suite: `sha256:40dc77fa44eeb9edb53777450b62cf4036407181e15880645868014ff198c712`
- Evidence runner: `sha256:1d94bdcd8d79fccbe801a626d0b3a1e3b7bd6dfe8b5a76cc200b9681f61ba852`
- Genesis readiness bundle: `sha256:911f8863499a7187669fb88e9468f5a5a638cfcf0aab96944b89cfa0faa085d5`
- Pending release candidate: `sha256:6e5664923499315f5a92456842d634f5084d66a629ed2ddf8e6ba77ef347b1a0`
- Founding constitution: `sha256:0a642c0c0768eebb7de5768428b64951cfc822655af85e8636cae79da2992a54`
- Lockfile: `sha256:3ca9ca9ddf8e1ac501a6d8cd3efd225e208229d0b2e4224b7fe5b90b23abc61c`

The refreshed source-tree digest is `0xf3975bb9cb49e31e8d6d023af674f4bb93782a042d6dc6bda130424f88b57f01` across 125 source files. The test-suite digest is `0xee88a5732bbd354af9ca6a0bf2033212556975fd95a5fef6b636b62906deaf85` across 41 test/config files. The public-projection implementation digest is `0xbcc5a26f35c72f01d98860084a53de4e25749f624bd9c413d77742455097bec6` across 31 projection, public-API, and arena-data files. The migration digest is `0x65200a2ac773b7e17f2f81006dc997cfa670fc522ee1f975d8b819a7aa9b0f52` across four migration/schema files. The 15-file Blaxel deployment-manifest digest is `0x6c845d07cb1e1dd46072a6e7fcbcdaf17b943beb56885bc8a1f7710acd01fd9b`; the six-file Blaxel image-source digest remains `0xd3cef723e16ae6bd03bd1b3566dae0cb8369acac8dc5ce43205422691d329fd1`.

## External and approval blockers

Local acceptance does not clear the genesis blockers: founding convention incomplete; release manifest incomplete; exact ownerless transaction unavailable; funding and 30-day reserve unverified; four Blaxel workspaces and Agent Drive unavailable; the implemented signed projection and exit-portability transports are not yet proven between deployed Blaxel workspaces; live adversarial/capacity/recovery proofs are incomplete; and explicit approval for irreversible/public/spend actions is absent.

Accordingly, no workspace/resource was created, no paid capacity was reserved, no contract was broadcast, no founding choice was made, no remote branch/PR was pushed, and nothing was publicly exposed. Season One additionally remains gated on continued prepaid funding, concentration review, agent-ratified infrastructure changes, and supported hardware-backed non-exportable signing.
