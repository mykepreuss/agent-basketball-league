# Founding Alpha R01-06 failed-closed result

> Status: `FAILED_CLOSED_BEFORE_PROVIDER_MUTATION_ARCHIVE_METADATA_DRIFT`
> Authorization: `ABL-FOUNDING-ALPHA-R01-06`
> Recorded: `2026-08-22T20:01:02-07:00` in `America/Vancouver`
> Workspace: `agent-basketball-league`
> Region: `us-was-1`

## Outcome

R01-06 stopped during its mandatory zero-mutation gate. Every bound commit, source freeze, launch plan, manifest set, image-source set, body-image source, resource plan, Drive plan, evidence file, runtime, provider inventory, quota, privacy, ACL, payment, top-up, Neon-capacity, and cost condition matched. The freshly generated body-program archive did not.

The authorization bound archive digest `0x43d9373baaa2bee8d0affe80aaa7e394d1c2af01f47102b66edfbeb2306d5569`. Preparation inside a mode-`0700` run directory with process umask `077` generated byte-identical application files but archive digest `0x90e37ca9861562d47494979806cd82b3640b9d2a6deccfca36c7599a2ffddfc4`. R01-06 therefore failed closed before the first image push.

No Blaxel image, Sandbox, Function, Job, Drive, preview, token, secret, Agent, Application, or Volume was created or modified. No Neon project was created. No model or Base call occurred, and the four-hour paid run clock never started.

## Unchanged provider inventories

Post-stop readback proved:

- zero Sandboxes, Functions, Jobs, Blaxel Agents, Applications, Agent Drives, Volumes, policies, and integration connections in the target workspace;
- exactly the seven historical images remained: `abl-body-sandbox-image`, `abl-stage-arena`, `abl-stage-core-api`, `abl-stage-fixed-broker`, `abl-stage-player-body`, `abl-stage-public-api`, and `abl-stage-storage-broker`;
- the unrelated `sandbox-openai` model route remained deployed;
- Neon still contained only Hummingbird (`snowy-darkness-52052673`); and
- the Blaxel console still showed USD 18.52, automatic and monthly top-up unconfigured/off, no payment method, and the USD 5.00 low-balance alert.

## Root cause and correction

The archive packager normalized timestamps but inherited input permission and ownership metadata from the caller. The existing reproducibility test repackaged one unchanged permission tree, so it could not detect cross-umask drift.

The narrow correction canonicalizes tar headers before compression:

1. directories are mode `0755`;
2. symlinks are mode `0777`;
3. executable regular files are mode `0755` and non-executable regular files are mode `0644`;
4. `agent/main.mjs` remains the required mode `0600`;
5. uid, gid, owner, group, and mtime are canonical; and
6. every modified header checksum is recomputed.

A regression test packages byte-identical trees with private and shared permission layouts and requires byte-identical output. A private-permission tree and a separately generated default-permission tree now reproduce archive digest `0x65a837f5040edb5d8508fc048a07bd90695ecb94169919a70fc92348fa1d734c`, size 15,390,692 bytes, 13,676 files, and 14,511 members. The thirteen image-source digests and manifest set remain unchanged.

The complete exact Node 24.18.0 pipeline passes 366 assertions across 78 files and 113 uncached tasks with stable result digest `0x88134695f20cb36c758328ae0d4f754255214945a1e386ece84c15109eea2acd`. The corrected implementation source digest is `0xf341e4ded56f7993e93b269c267f88fde104b296532227b10126c513d1a02e2b`; the corrected private resource-plan file digest is `0x5ddef156ef441ecd75598c49c9f07f6772f3de43e722f110d602dfb8f627c961`; and the derived launch-ledger digest is `0x3caeba115db15ec5b1ac7187b3af5e868ed9df887afd80d9e0841e77be12cda1`.

R01-06 is consumed and cannot be reused. Merge this result and correction first, calculate every dependent digest from final `main`, record a new read-only Preflight 09 without modifying a bound dependency, and obtain a replacement authorization before any provider mutation.
